#include <stdio.h>

#define MAXCHAR 1024

double average (double a, int b, int n);

double lettersPerWord (unsigned int hex[MAXCHAR]);

/* nic, aq vamo comecar usando array e qsort ja implementadas
 * pra acelerar o processo
 * depois a gnt pode colocar uma arvore de busca binaria pra deixar
 * o processo eficiente com listas encadeadas */
void ordenaEstatistica ();
